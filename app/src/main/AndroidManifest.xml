<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.andy.sep">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Survey.Q3Activity" />
        <activity android:name=".Survey.Q2Activity" />
        <activity android:name=".Survey.Q1Activity" />
        <activity android:name=".Student" />
        <activity android:name=".Admin" />
        <activity android:name=".Recruiter" />
        <activity
            android:name=".userlist"
            android:label="@string/title_activity_userlist"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".changeDetails" />
        <activity
            android:name=".ManageClass"
            android:label="@string/title_activity_manage_class"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".changeClassDetails"></activity>
    </application>

</manifest>